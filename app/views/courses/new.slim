- provide :title, t(".title")
.col-lg-6
 
  .row.from_login
    .col-md-9.col-md-offset-1
      .panel.panel-default
        .panel-heading
          h2.center= t ".h1_html"
        .panel-body
          = form_for(@course) do |f|
            fieldset
              .form-group
                = f.label :name, t("courses.new.name") 
                = f.text_field :name
                |
                = f.label :description, t("courses.new.description")
                = f.text_field :description
              = f.submit "Tạo khóa học", class: "btn btn-primary"
    .col-md-3.col-md-offset-1.login_img
      img.img-fluid alt=("Logo Login") src="/assets/logo_login1.svg"
